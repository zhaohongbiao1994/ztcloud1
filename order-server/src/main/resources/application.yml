server:
  port: 10000
spring:
  datasource:
    url: jdbc:mysql://101.201.140.42:3306/zt_study?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2b8&useSSL=false&tinyInt1isBit=false
    username: root
    password: 1994@04
    driver-class-name: com.mysql.jdbc.Driver
  #application:
   # name: orderservice
  #cloud:
   # nacos:
    #  server-addr: localhost:8848 #nacos服务地址
     # discovery:
      #  cluster-name: BJ
       # namespace: bf4fbd57-c4f8-46d7-bdb4-cf6de941ec83 #命名空间
        #ephemeral: true #是否是临时实例
mybatis:
  type-aliases-package: com.zh.order.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    com.zh: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
#eureka:
#  client:
#    service-url:  # eureka的地址信息
#      defaultZone: http://127.0.0.1:10086/eureka
#配置负载均衡策略
userservice:
  ribbon:
   # NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule  # 负载均衡规则  随机
    NFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule  # 负载均衡规则

#开启饥饿加载
ribbon:
  eager-load:
    enabled: true
    clients: #指定饥饿加载的服务名称
      - userservice